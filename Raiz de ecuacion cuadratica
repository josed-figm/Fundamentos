#include <iostream>
#include <cmath>
using namespace std;


int main()
{
    float a,b,c;
    cout<<"Registrar valor de a:";
    cin>>a;
      cout<<"Registrar valor de b:";
    cin>>b;
   cout<<"Registrar valor de c:";
    cin>>c;
    double x1,x2;
    x1=(-b-sqrt(b*b-4*a*c)/2*a);
    x2=(-b+sqrt(b*b-4*a*c)/2*a);
    cout<<"El valor de la primer raiz x1 es:"<<x1;
    cout<<endl<<"El valor de la primer raiz x2 es:"<<x2;


    return 0;
}
