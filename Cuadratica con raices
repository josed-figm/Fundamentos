#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float a,b,c,d;
    double x1,x2;
    cout<<"Ingresar el valor de a: ";
    cin>>a;
    if (a==0) 
     cout<<"ERROR: No se puede calcular";
     else {
        cout<<"Ingresar el valor de b: ";
        cin>>b;
        cout<<"Ingresar el valor de c: ";
        cin>>c;
        d=b*b-4*a*c;
        if (d==0)
           cout<<"La solucion es unica: "<<-b/(2*a);
        else 
            if (d>0) {
                x1=(-b-sqrt(b*b-4*a*c)/2*a);
                x2=(-b+sqrt(b*b-4*a*c)/2*a);
                cout<<"El valor de la primer raiz x1 es: "<<x1<<endl;
                cout<<"El valor de la primer raiz x2 es: "<<x2;}
            else {
                x1=-b/(2*a);
                x2= sqrt(-d)/(2*a);
                cout<<"El valor de la primer raiz x1 es: "<<x1<<"+"<<x2<<"i"<<endl;
                cout<<"El valor de la primer raiz x2 es: "<<x1<<"-"<<x2<<"i";}
            }
        
                
        


    return 0;
}
